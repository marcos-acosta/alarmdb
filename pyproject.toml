[project]
name = "alarmdb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "lark>=1.3.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-sugar>=1.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
